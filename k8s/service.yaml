apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort
  selector:
    app: flask
  ports:
    - protocol: TCP
      port: 5000        # service port inside cluster
      targetPort: 5000  # pod port
      nodePort: 30000   # accessible from outside via node IP